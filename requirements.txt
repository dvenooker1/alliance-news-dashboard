# Python dependencies for the daily build (scripts/build.py).
# Installed by the GitHub Actions workflow before each run.
requests>=2.31
feedparser>=6.0
anthropic>=0.70        # only used when ANTHROPIC_API_KEY is set; harmless otherwise
tzdata>=2024.1         # ensures America/New_York is available on minimal runners
